/**
 * Starter surface for `@openlance/aios-{{kebabCase name}}`. Replace this with the
 * package's real implementation. It exists so a freshly scaffolded package builds,
 * type-checks, tests at full coverage, and benchmarks with no manual edits, and it
 * gives the barrel a real module to re-export (exercising the declaration build).
 */
export const {{camelCase name}}Ready = (): boolean => true;
