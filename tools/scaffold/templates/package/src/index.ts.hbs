/**
 * @packageDocumentation
 * `@openlance/aios-{{kebabCase name}}`
 *
 * {{description}}
 *
 * This file is the single supported public API (Engineering Rule 1). Internal
 * implementation lives under `src/internal` and is never re-exported. Consumers
 * import only from the package entry point.
 *
 * Constitution: {{constitution}}
 */

export {};
