{
  "name": "@openlance/aios-seo-intelligence",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Phase 5 Stage 3 SEO Intelligence: the third AI Growth OS Feature, a deterministic domain subsystem that owns the SEO behavior only (ADR-0051). The Content -> SEO step of the growth chain: it consumes a Marketing Intelligence direction and a Content Intelligence output (both by reference), plus knowledge by reference, and frames a governed SEO task: an immutable SeoPlan carrying the frozen Agent Engine AgentRequest (prompt, provider steps), plus an SEO-quality EvaluationRequest for the frozen Evaluation Engine. It is deterministic and fail-closed; it never owns business truth, never executes, invokes a provider, selects a model, retrieves, crawls, indexes, scores, or decides, and builds no new platform infrastructure.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --sourcemap --tsconfig tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage",
    "bench": "vitest bench --run",
    "docs": "typedoc src/index.ts --out docs-api"
  },
  "dependencies": {
    "@openlance/aios-agent-engine": "workspace:*",
    "@openlance/aios-content-intelligence": "workspace:*",
    "@openlance/aios-di": "workspace:*",
    "@openlance/aios-errors": "workspace:*",
    "@openlance/aios-evaluation-engine": "workspace:*",
    "@openlance/aios-kernel": "workspace:*"
  },
  "devDependencies": {
    "@openlance/aios-composition-root": "workspace:*",
    "@openlance/aios-config": "workspace:*",
    "@openlance/aios-logging": "workspace:*",
    "@openlance/aios-tsconfig": "workspace:*",
    "@vitest/coverage-v8": "^2.1.8",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "aios": {
    "layer": "app",
    "constitution": [],
    "stability": "Experimental",
    "apiVersion": "0.0.0",
    "generation": 1
  }
}
