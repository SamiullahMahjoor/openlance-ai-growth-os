{
  "name": "@openlance/aios-memory-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Phase 4 Stage 3 Memory Engine: the Runtime's foundational operational memory subsystem, the operational realization of the frozen Memory namespace. It carries out operational memory management (form: receive/classify/validate/retain; recall: make retained context available; remove) over retained records, driving the frozen @openlance/aios-memory model. It consumes only the frozen memory model and the substrate, depends on no operational service, indexes and recalls deterministically and structurally (never retrieval / RAG / semantic search / embeddings), executes nothing, and contains no vendor knowledge (ADR-0037).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --sourcemap --tsconfig tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage",
    "bench": "vitest bench --run",
    "docs": "typedoc src/index.ts --out docs-api"
  },
  "dependencies": {
    "@openlance/aios-di": "workspace:*",
    "@openlance/aios-errors": "workspace:*",
    "@openlance/aios-events": "workspace:*",
    "@openlance/aios-kernel": "workspace:*",
    "@openlance/aios-memory": "workspace:*",
    "@openlance/aios-plugins": "workspace:*"
  },
  "devDependencies": {
    "@openlance/aios-composition-root": "workspace:*",
    "@openlance/aios-config": "workspace:*",
    "@openlance/aios-logging": "workspace:*",
    "@openlance/aios-tsconfig": "workspace:*",
    "@vitest/coverage-v8": "^2.1.8",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "aios": {
    "layer": "app",
    "constitution": [
      "OL-AI-MEMORY-README"
    ],
    "stability": "Experimental",
    "apiVersion": "0.0.0",
    "generation": 1
  }
}
